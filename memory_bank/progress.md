# Progress

## Current Status

Документация и архитектурный контур инициализированы. Локальный Git настроен и синхронизирован с удаленным репозиторием. Реализованы teacher control room, Appwrite schema provisioning, CRUD для `students` и `projects`, запись посещаемости и project detail actions. Дополнительно активирован student-access сценарий: bind GitHub-аккаунта по `github_user_id` после Telegram-подтверждения и student-only управление своей проектной линией на `/my-project`. Teacher-only AI-анализ проектов теперь опирается на данные `memory_bank` и commit history student GitHub repository, а не только на локально заполненные поля проекта. В текущей сессии модель проектов расширена до групповой работы: один GitHub-репозиторий хранится одним `project`-документом, участие нескольких учеников вынесено в `project_memberships`, teacher-only detail page проекта управляет составом группы, а student/teacher counters и отчеты учитывают memberships. Измененные кодовые файлы уже прогнаны через `bunx biome check --write`, `bun run lint` и `bun run build` проходят успешно.

## Known Issues

- в рабочем дереве присутствует локальный `.env`, поэтому он должен оставаться вне версии;
- для `Appwrite Cloud Free` автоматическая профилактика паузы через API не выглядит надежной, потому что Appwrite описывает inactivity rule через Console activity; текущая защита остается operational-процессом, а не техническим bypass;
- в локальной Windows-среде порт `3100` входит в системный excluded TCP range `3068-3167`, поэтому `bun dev` на нем не стартует даже при bind на `127.0.0.1`;
- лимиты Appwrite на размер строковых атрибутов требуют держать `projects` и `project_ai_reports` в компактной JSON-state схеме;
- даже после нормализации `project_state_json` нужно контролировать суммарный размер JSON при дальнейшем расширении AI summary и списков шагов.
- для Telegram linking flow нужно сохранять синхронность `TELEGRAM_WEBHOOK_SECRET` между Vercel env и настройкой webhook у бота; при рассинхроне Telegram будет получать `401` от `/api/telegram/webhook`.
- локальный `.env` не содержит `TELEGRAM_WEBHOOK_SECRET`, поэтому валидный production webhook smoke test с корректным секретом из терминала в этой среде недоступен.
- в production/Appwrite коллекция `projects` зависит от student bind flow и выбора репозитория на `/my-project`, поэтому teacher-only сценарии `/projects`, GitHub sync и AI-analysis требуют хотя бы один подключенный student project; проблема не в отсутствии Appwrite-коллекций или схемы.
- student-access bind flow теперь зависит от `NEXTAUTH_URL`: без корректного публичного URL Telegram-бот не сможет выдать рабочую GitHub login-ссылку после `Start`.
- production teacher login теперь требует `TEACHER_GITHUB_USER_ID`; если переменная не задана, teacher-доступ считается не настроенным даже при наличии `TEACHER_GITHUB_LOGIN`.
- commit metrics сейчас считаются по выборке последних commit pages GitHub API, поэтому для очень больших репозиториев частота и количество отражают актуальное рабочее окно, а не бесконечную историю всего проекта.
- без `GITHUB_TOKEN` teacher-only AI-analysis и GitHub sync быстро упираются в публичный rate limit GitHub API; временный `403 rate limit exceeded` теперь диагностируется отдельно, но для стабильных прогонов нужен token.
- Workers AI на Cloudflare Free ограничен дневной квотой `10,000 neurons/day`, поэтому автоматический batch AI-analysis по расписанию отключен; до возможного перехода на Paid запуск остается ручным.
- при новых задачах, завязанных на AI-анализ или AI-report generation, нужно учитывать operational fallback на Hugging Face API, потому что Cloudflare Workers AI может снова упереться в дневную квоту раньше конца рабочего дня.
- для legacy-проектов без документов в `project_memberships` используется fallback на owner `projects.student_id`; пока отдельная data-migration не запускалась, этот compatibility-слой нужно сохранять.

## Changelog

- 2026-03-26: на teacher-only detail page проекта добавлен явный режим группового проекта. В `src/lib/project-limits.ts`, `src/lib/server/mappers.ts`, `src/lib/server/repositories/projects.ts` и `src/lib/types.ts` введен и протянут сохраняемый флаг `isGroupProject` внутри `project_state_json`. В `src/app/projects/actions.ts` добавлен server action переключения этого режима, а в `src/app/projects/[projectId]/page.tsx` в owner-row слева от badge `Owner` добавлен switch `В групповой проект`. Блок `Добавить ученика в групповой проект` теперь показывается только когда этот флаг включен. Для preview student page в `src/app/my-project/page.tsx` обновлены mock `ProjectRecord`. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run lint` и `bun run build` проходят.
- 2026-03-26: исправлен источник данных для attendance summary `Требуют внимания`. В `src/lib/server/repositories/attendance.ts` список `studentsNeedingAttention` больше не зависит от заранее рассчитанного `student.weeklyState`; теперь он вычисляется по реальным attendance states строк выбранной недели с тем же правилом `attendanceRate < 100`. В `src/lib/server/attendance-report.ts` summary-count и список `Зона внимания` дополнительно переведены на расчет из уже собранных report rows, поэтому printable/share attendance report больше не должен показывать `0`, если в таблице есть ученики с посещаемостью ниже `100%`. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run lint` и `bun run build` проходят.
- 2026-03-26: выполнен UI-polish attendance/report surfaces. В `src/app/attendance/attendance-weekly-report-card.tsx` удален служебный explanatory copy (`PDF-вид отчета`, teacher-only описание и поясняющий абзац) и оставлены только action-кнопки. В `src/app/attendance/attendance-grid-client.tsx` кнопка `Очистить отметки` перенесена в общий ряд действий рядом с сохранением и переключением недель. В `src/components/app/teacher-shell.tsx` theme toggle перенесен в правый верхний угол общего header, из `src/components/app/teacher-sidebar.tsx` удален его старый sidebar-вариант, а в `src/app/attendance/report/attendance-report-view.tsx` и `src/app/projects/report/project-report-view.tsx` тот же toggle добавлен в верхнюю панель printable/report view. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run lint` и `bun run build` проходят.
- 2026-03-26: логика weekly attendance state приведена к product-правилу `меньше 100% = требует внимания`. В `src/lib/server/repositories/students.ts` helper `buildWeeklyState()` для attendance summaries больше не использует пороги `25/75`; теперь `0%` дает `critical`, `1-99%` дает `warning`, а только `100%` считается `success`. Это синхронизирует `studentsNeedingAttention`, PDF-отчет `/attendance/report`, страницу `/attendance` и attendance badges в teacher UI. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` и `bun run build` проходят.
- 2026-03-26: в `src/app/attendance/attendance-grid-client.tsx` порядок кнопок в modal подтверждения несохраненных attendance-изменений скорректирован под прямое требование пользователя: `Сохранить`, `Не сохранять`, `Отмена`. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` и `bun run build` проходят.
- 2026-03-26: на `/attendance` заменен пассивный баннер о несохраненных изменениях на активный navigation guard. В `src/app/attendance/attendance-grid-client.tsx` добавлен перехват внутренних переходов по ссылкам при `isDirty` и кастомное модальное окно `Сохранить / Не сохранять / Отмена` на базе `FeedbackModal`; стандартный `beforeunload`-guard отключен по явному запросу пользователя. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` и `bun run build` проходят.
- 2026-03-26: на `/attendance` добавлено lesson-level состояние `Не состоялось`. В `src/app/attendance/attendance-grid-client.tsx` это состояние встроено четвертым шагом в цикл по клику на заголовок weekday-колонки: `Нет данных -> Отсутствовал -> Присутствовал -> Не состоялось`. По уточненному требованию заголовок дня недели не переименовывается даже в состоянии отмененного урока; вместо этого рядом с описанием недели и action-кнопками добавлена легенда с цветовой расшифровкой всех состояний. В `src/app/attendance/report/attendance-report-view.tsx` такая же легенда добавлена в PDF-отчет. Ячейки отмененного урока блокируются, а сохранение идет через `lessons.is_closed`. В `src/lib/server/repositories/attendance.ts` добавлен server-side helper обновления lesson status с очисткой attendance-записей по закрытому уроку, а расчет в `src/lib/server/attendance-report.ts` теперь исключает такие уроки из weekly attendance rate и помечает их в PDF-отчете. Архитектурная документация и memory bank синхронизированы. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run lint` и `bun run build` проходят.
- 2026-03-24: реализован механизм групповой работы над одним проектом. В Appwrite provisioning добавлена и применена коллекция `project_memberships` через `bun run db:provision`, `projects.student_id` переосмыслен как owner GitHub-репозитория, а чтение проектов и student counters переведены на memberships с fallback для legacy-записей. Teacher-only `/projects` теперь показывает проекты по одной строке с участниками группы, detail page `/projects/[projectId]` умеет добавлять и удалять участников, student `/my-project` и teacher `/students/[studentId]` показывают групповые проекты, а project reports и weekly project summary учитывают одного project record на нескольких учеников. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run lint` и `bun run build` проходят.
- 2026-03-24: по явному запросу пользователя `package.json` dev script возвращен с `127.0.0.1:3300` обратно на `localhost:3000`. Измененный не-Markdown файл прогнан через `bunx biome check --write package.json`, повторная проверка проходит.
- 2026-03-24: завершено техническое оздоровление auth/config flow по итогам code review. В репозитории добавлен общий cleanup helper `src/lib/server/project-cleanup.ts`, и теперь `deleteStudent()` и `deleteProject()` каскадно удаляют связанные `project_ai_reports` перед удалением `projects`. Студенческий flow `/my-project` больше не позволяет выбрать приватный GitHub-репозиторий: такие карточки отображаются красным с меткой `Приватный`, кнопка выбора отключается, а server-side action повторно сверяет список репозиториев и отвергает подмену formData. Auth guard перенесен из `src/middleware.ts` в `src/proxy.ts`, project share secret развязан на `PROJECT_REPORT_SHARE_SECRET` с legacy fallback на `ATTENDANCE_REPORT_SHARE_SECRET`, а `package.json` dev script переведен на `127.0.0.1:3300`. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, затем успешно прошел `bun run build`.
- 2026-03-21: на `src/app/projects/[projectId]/page.tsx` уточнен formatter подписи `Последний коммит`. Если commit age меньше часа, detail page теперь показывает `N мин. назад`; если меньше суток — `N ч. назад`; fallback `0 дн. назад` убран и больше не должен появляться при свежих коммитах. Измененный не-Markdown файл прогнан через `bunx biome check --write`, повторная проверка проходит.
- 2026-03-21: исправлен канонический поток подготовки student-репозиториев под `Project Deliverables`. В `AGENTS.md` закреплены обязательные правила для `## Project Deliverables`: всегда существующий раздел в `memory_bank/projectbrief.md`, только табличный формат `ID | Deliverable | Status | Weight`, только канонические статусы `pending/in_progress/completed/blocked` и сумма весов ровно `100`. В `src/lib/server/canonical-agents.ts` добавлен server-only helper, который подтягивает актуальный `AGENTS.md` из `https://github.com/Ravva/projects-tracker/blob/main/AGENTS.md` через raw GitHub URL с локальным fallback. Student-onboarding на `/my-project` обновлен: страница показывает кнопку копирования канонического `AGENTS.md`, обновленный setup prompt и встроенный self-check для `memory_bank/projectbrief.md` и `Project Deliverables`. Server-side AI snapshot теперь сохраняет явную причину, почему progress не считается корректно (`нет projectbrief`, нет `Project Deliverables`, deliverables не распарсились, сумма весов не равна `100`), а teacher-only detail page `/projects/[projectId]` показывает эту диагностику отдельной карточкой. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, повторная проверка Biome проходит, `bun run build` проходит успешно.
- 2026-03-21: название проекта в printable project report сделано кликабельным. В `src/lib/server/project-report.ts` в `ProjectReportItem` и `ProjectReportRow` добавлено поле `projectUrl`, а builder отчета теперь заполняет его из `project.githubUrl`. В `src/app/projects/report/project-report-view.tsx` добавлен helper `renderProjectLink()`, и название проекта рендерится как ссылка на GitHub как в таблице `По ученикам`, так и в списках `Хорошая динамика / Нет динамики / Заброшенные проекты`. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, повторная проверка проходит.
- 2026-03-21: в `src/app/projects/report/project-report-view.tsx` текст пустого статуса в столбце `Статус недели` printable project report приведен к формату `Нет данных`. Измененный не-Markdown файл прогнан через `bunx biome check --write`, повторная проверка проходит.
- 2026-03-21: в `src/app/projects/report/project-report-view.tsx` уточнена расшифровка нейтрального статуса в столбце `Статус недели` printable project report. Вместо общего текста `Есть данные` добавлен helper `renderWeeklyStatusLabel()`, который показывает `Недостаточно данных` при `progressDelta === null`, `Снижение прогресса` при отрицательной динамике и `Слабая динамика` для небольшого положительного изменения. Измененный не-Markdown файл прогнан через `bunx biome check --write`, повторная проверка проходит.
- 2026-03-21: в `src/app/projects/report/project-report-view.tsx` зафиксирована геометрия цветной точки в столбце `Статус недели` printable project report. Индикатор получил `shrink-0`, `self-center` и `aspect-square`, поэтому круг больше не должен растягиваться по вертикали при высоких строках таблицы. Измененный не-Markdown файл прогнан через `bunx biome check --write`, повторная проверка проходит.
- 2026-03-21: в `src/lib/server/project-report.ts` упрощен текст для столбца `Последнее обновление` в printable project report. `formatLastUpdateLabel()` больше не добавляет префикс `обновление`, поэтому в PDF-отчете значения выводятся как `сегодня`, `1 дн. назад` и `N дн. назад`. Измененный не-Markdown файл прогнан через `bunx biome check --write`, повторная проверка проходит.
- 2026-03-21: printable project report на `/projects/report` доработан под новый teacher-format. В `src/app/projects/report/project-report-view.tsx` в summary-блок `Отчет по проектам` добавлен пункт `Нет проектов`, секции переставлены в порядок `По ученикам -> Хорошая динамика -> Нет динамики -> Заброшенные проекты -> Отсутствуют данные о проекте`, а заголовок таблицы обновлен на `Фамилия Имя`. В `src/lib/server/project-report.ts` формат student name переведен с `Фамилия, имя` на `Фамилия Имя` без запятой для всего PDF-отчета. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, повторная проверка проходит.
- 2026-03-21: sidebar очищен от неиспользуемого блока `Рабочий поток`. В `src/components/app/teacher-sidebar.tsx` удалены секция `Рабочий поток`, ее пункты-назаглушки и лишние импорты иконок; в teacher sidebar осталась только основная навигация по реальным разделам. Измененный не-Markdown файл будет прогнан через `bunx biome check --write`, затем `bun run build`.
- 2026-03-21: исправлена инвертированная логика mobile header scroll behavior. В `src/components/app/teacher-shell.tsx` условие направления скролла перевернуто: теперь при движении страницы вниз header скрывается, а при движении вверх снова показывается, что соответствует ожидаемому мобильному UX. Измененный не-Markdown файл будет прогнан через `bunx biome check --write`, затем `bun run build`.
- 2026-03-21: выпадающие меню сделаны читаемыми на сложном фоне. В `src/components/ui/dropdown-menu.tsx` `DropdownMenuContent` получил явный непрозрачный фон `bg-[hsl(var(--popover))]`, border и усиленную тень вместо визуально полупрозрачной панели; это должно исправить нечитаемость меню `Быстрые действия` и других dropdown-элементов в обеих темах. Измененный не-Markdown файл будет прогнан через `bunx biome check --write`, затем `bun run build`.
- 2026-03-21: mobile header сделан более минималистичным. В `src/components/app/teacher-shell.tsx` на маленьких экранах скрыт eyebrow `Academic Control Room`, уменьшены заголовок, отступы и расстояния между элементами, чтобы верхний nav-bar занимал меньше вертикального пространства, сохраняя desktop-вариант без изменений. Измененный не-Markdown файл будет прогнан через `bunx biome check --write`, затем `bun run build`.
- 2026-03-21: teacher navigation упрощена, а mobile header получил scroll-aware поведение. В `src/components/app/teacher-sidebar.tsx` удален пункт `Дашборд`, в `src/components/app/teacher-dashboard.tsx` убран быстрый CTA `Appwrite Console`, а `src/components/app/teacher-shell.tsx` теперь на мобильной ширине отслеживает направление скролла и по текущему UX-правилу скрывает header при прокрутке вверх, возвращая его при прокрутке вниз. Измененные не-Markdown файлы будут прогнаны через `bunx biome check --write`, затем `bun run build`.
- 2026-03-21: переключатель темы перенесен в footer sidebar. В `src/components/app/teacher-shell.tsx` он убран из общего header, а `src/components/app/teacher-sidebar.tsx` теперь показывает его над карточкой пользователя. Заодно collapsed-state footer и brand-block переведены на фиксированные квадратные размеры с центрированием: логотип проекта, аватар преподавателя и кнопка `Выйти` больше не должны сжиматься при `collapsible=icon`; кнопка выхода получила иконку `Logout01`. Измененные не-Markdown файлы будут прогнаны через `bunx biome check --write`, затем `bun run build`.
- 2026-03-21: из локальной server console убран auth debug spam. В `src/lib/server/auth.ts` удалены ручные диагностические `console.log("[Auth] ...")`, а `NextAuthOptions` больше не включает `debug: true`, поэтому предупреждение `[next-auth][warn][DEBUG_ENABLED]` и повторяющиеся строки про `GITHUB_ID/GITHUB_SECRET/NEXTAUTH_URL/TEACHER_GITHUB_LOGIN` больше не должны появляться при server render. Измененный не-Markdown файл будет прогнан через `bunx biome check --write`, затем `bun run build`.
- 2026-03-21: устранен сбой `AI gateway вернул невалидный JSON` после переключения Cloudflare на `@cf/qwen/qwen3-30b-a3b-fp8`. В `workers/ai-worker/src/index.ts` включен JSON Mode через `response_format: { type: "json_object" }`, `DEFAULT_MODEL` синхронизирован с Qwen3, а `getAiContent()` теперь умеет сериализовать object-shaped ответы Workers AI. В `src/lib/server/ai-gateway.ts` extractor дополнительно срезает reasoning-блоки `<think>...</think>` и разворачивает JSON-обертку `response`, если модель возвращает ее вместо плоского объекта. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run build` проходит, обновленный Worker задеплоен через `bunx wrangler deploy -c workers/ai-worker/wrangler.toml`, а живой smoke test gateway вернул корректный JSON `{\"ok\": true}`.
- 2026-03-21: default Cloudflare-модель для teacher-only AI-анализа переведена на `@cf/qwen/qwen3-30b-a3b-fp8`. Синхронизированы локальный `.env`, шаблон `.env.example`, кодовый default `getAiGatewayModel()` в `src/lib/server/ai-gateway.ts` и архитектурные заметки, чтобы runtime больше не оставался на `@cf/openai/gpt-oss-120b` после предыдущего решения перейти на Qwen3.
- 2026-03-21: в teacher-only feedback по AI-анализу добавлен индикатор фактического AI-провайдера. `src/lib/server/ai-gateway.ts` теперь возвращает не только JSON, но и metadata `providerCode/modelName`; `src/lib/server/repositories/projects.ts` сохраняет в `project_ai_reports.model_name` реальную связку `CF/HF + model` и возвращает provider в result `runProjectAiAnalysis()`. В `src/app/projects/actions.ts`, `src/app/projects/page.tsx`, `src/app/projects/[projectId]/page.tsx` success-banner после sync/analysis и batch summary показывают `CF/HF`, чтобы было видно, через Cloudflare или Hugging Face прошел AI-анализ. Измененные не-Markdown файлы будут прогнаны через `bunx biome check --write`, затем выполняется `bun run build`.
- 2026-03-21: локально временно отключен Hugging Face fallback. В `.env` очищен `HF_TOKEN`, поэтому текущая dev-среда больше не использует HF как runtime fallback, хотя сама кодовая поддержка fallback в репозитории сохранена. Изменены только локальный `.env` и Markdown-файлы памяти проекта.
- 2026-03-21: teacher-only project detail уточнен по формату давности последнего коммита. В `src/app/projects/[projectId]/page.tsx` `formatLastCommitLabel()` теперь показывает интервалы меньше часа в минутах (`N мин. назад`) вместо прежнего округления до `1 ч. назад`. Измененный не-Markdown файл прогнан через `bunx biome check --write`, повторная проверка проходит.
- 2026-03-21: teacher-only shell получил ручной переключатель темы. Добавлены `src/components/theme/theme-provider.tsx` и `src/components/theme/theme-toggle.tsx`, `src/app/layout.tsx` теперь инициализирует тему до гидратации, а `src/app/globals.css` поддерживает управляемые режимы `Системная / Светлая / Темная` через `data-theme` с сохранением fallback на системную тему. В `src/components/app/teacher-shell.tsx` theme switcher вынесен в общий header, а мобильный sidebar в `src/components/ui/sidebar.tsx` и `src/components/app/teacher-sidebar.tsx` больше не прячет кнопку закрытия, явно закрывается кнопкой `Скрыть` и автоматически сворачивается после перехода по навигации. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run build` проходит.
- 2026-03-21: переключатель темы упрощен по UI. `src/components/theme/theme-toggle.tsx` больше не открывает dropdown и теперь рендерит компактную круглую кнопку с иконкой `Солнце/Луна`, которая переключает только между светлой и темной темой, сохраняя системную тему как состояние по умолчанию до первого ручного переключения. Измененный не-Markdown файл будет повторно прогнан через `bunx biome check --write`, затем `bun run build`.
- 2026-03-21: fallback на Hugging Face расширен и на сетевые сбои Cloudflare gateway. В `src/lib/server/ai-gateway.ts` `shouldFallbackToHuggingFace()` теперь включает `fetch failed` и типовые network errors (`ECONNRESET`, `ECONNREFUSED`, `ENOTFOUND`, timeout), чтобы teacher-side AI-analysis уходил в HF не только при quota error `4006`, но и при сетевой недоступности Cloudflare gateway. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run build` проходит.
- 2026-03-21: Hugging Face fallback уточнен после живой проверки токена. `Qwen/Qwen2.5-7B-Instruct-1M` возвращал `model_not_supported`, тогда как `Qwen/Qwen2.5-7B-Instruct` отвечал успешно. Поэтому в `src/lib/server/ai-gateway.ts` default HF model переключен на `Qwen/Qwen2.5-7B-Instruct`, а fallback дополнился retry-цепочкой по нескольким HF моделям. `.env.example` и локальный `.env` синхронизированы с новым default. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run build` проходит.
- 2026-03-21: server-only AI client получил fallback на Hugging Face. В `src/lib/server/ai-gateway.ts` добавлен OpenAI-compatible вызов `https://router.huggingface.co/v1/chat/completions`, который включается при quota error `4006` от Cloudflare Workers AI, а также при `AI_FORCE_HF=true` или отсутствии `AI_GATEWAY_*` конфига при наличии `HF_TOKEN`. В `.env.example` и локальный `.env` добавлены `HF_TOKEN`, `HF_BASE_URL`, `HF_CHAT_MODEL`, `AI_FORCE_HF`; архитектурная документация и memory bank синхронизированы. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run build` проходит.
- 2026-03-20: в memory bank зафиксировано operational-решение по AI-провайдерам. Для следующих задач, связанных с AI-анализом и AI-report generation, нужно заранее учитывать fallback на Hugging Face API, так как Cloudflare Workers AI / Workers Free ограничены дневной квотой и уже упирались в исчерпание лимита. Изменены только Markdown-файлы памяти проекта.
- 2026-03-20: teacher-only таблица `/projects` получила финальный visual polish. В `src/app/projects/projects-table.tsx` заглушки `отсутствует` заменены на `-`, размер текста таблицы поднят до `text-sm/md:text-base`, а сам `Table` теперь рендерится как `w-max`, чтобы ширина колонок определялась содержимым, а не растягивалась на весь контейнер. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run build` проходит.
- 2026-03-20: внутренний вертикальный скролл таблиц убран на уровне общего UI-компонента. В `src/components/ui/table.tsx` wrapper `data-slot="table-container"` изменен с `overflow-x-auto` на `overflow-x-auto overflow-y-hidden`, чтобы длинные таблицы, включая `/projects`, больше не показывали отдельную вертикальную полосу прокрутки внутри контейнера. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run build` проходит.
- 2026-03-20: teacher-only таблица `/projects` получила отдельный столбец `Последнее изменение`. В `src/app/projects/projects-table.tsx` дата последнего обновления вынесена из ячейки проекта в отдельную колонку и форматируется как `дд.мм.гг чч:мм`; первый столбец снова называется `Ученик`, а в `src/app/projects/page.tsx` имя ученика теперь строится как `Фамилия Имя` без запятой. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run build` проходит.
- 2026-03-20: teacher-only таблица `/projects` переведена на полный список учеников. В `src/app/projects/page.tsx` данные теперь собираются из `listStudents()` и текущих проектов по `studentId`, а `src/app/projects/projects-table.tsx` рендерит 25 строк в алфавитном порядке по `Фамилия, имя`. Для учеников без текущего проекта столбцы `Проект`, `Статус` и `Прогресс` явно показывают `отсутствует`; кликабельность строки сохраняется только для учеников с проектом. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run build` проходит.
- 2026-03-20: teacher-only таблица `/projects` получила более читаемый текст статуса. В `src/app/projects/projects-table.tsx` метка `Последний snapshot` заменена на `Последние изменения` с форматированием через `Intl.DateTimeFormat("ru-RU", { dateStyle: "medium", timeStyle: "short", timeZone: "Europe/Moscow" })`, а в `src/lib/server/repositories/projects.ts` убрана служебная подпись `Локальный snapshot совпадает с GitHub default branch.` для `synced`-состояния. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run build` проходит.
- 2026-03-20: teacher-only список `/projects` упрощен. Новый client-компонент `src/app/projects/projects-table.tsx` рендерит полноширинную таблицу с колонками `Ученик / Проект / Статус / Repo sync / AI / Риск / Прогресс`, открывает detail page по клику на строку и поддерживает keyboard navigation. В `src/app/projects/page.tsx` удалены колонка `Действие` и боковой блок `Фокус review`, поэтому таблица теперь занимает всю ширину страницы. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run build` проходит.
- 2026-03-20: printable project report дополнительно приведен к формату attendance table. В `src/lib/server/project-report.ts` добавлены отсортированные строки по всем ученикам с форматом имени `Фамилия, имя`, а `src/app/projects/report/project-report-view.tsx` теперь рендерит общую таблицу `Фамилия, имя / Проект / Прогресс / Динамика / Последнее обновление / Статус недели`, где для учеников без текущего проекта явно показывается `данных нет`. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run build` проходит.
- 2026-03-20: project report переведен на printable/share-flow по аналогии с attendance. Добавлены `src/lib/server/project-report.ts`, teacher-only route `/projects/report`, public signed route `/project-report/share`, годовая share-ссылка на общем секрете `ATTENDANCE_REPORT_SHARE_SECRET`, блоки `Хорошая динамика`, `Нет динамики`, `Заброшенные проекты`, `Отсутствуют данные о проекте`, а формулировка `коммит` в отчете заменена на `обновление`. На `/projects` теперь можно открыть PDF-view и скопировать share-ссылку для родителей. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run build` проходит.
- 2026-03-20: attendance report получил отдельный signed share-flow для родителей. Добавлен секрет `ATTENDANCE_REPORT_SHARE_SECRET`, server-only helper подписи ссылки, public route `/attendance/report/share` без auth, а teacher-only `/attendance/report` сохраняет кнопку возврата и отдельные кнопки печати/сохранения PDF. На `/attendance` появилась кнопка копирования годовой share-ссылки для родителей. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run build` проходит.
- 2026-03-20: attendance reporting переведен из копируемого текста в отдельную teacher-only report page с PDF-like версткой. Новый `src/lib/server/attendance-report.ts` готовит структурированные данные по неделе, route `src/app/attendance/report/page.tsx` рендерит A4-like представление с таблицей, цветными индикаторами и кнопкой `Печать / Сохранить PDF`, а карточка на `/attendance` теперь открывает этот PDF-view вместо plain text/copy-flow. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run build` проходит.
- 2026-03-20: формат teacher-only attendance report скорректирован под реальное поведение Telegram. В `src/lib/server/attendance-weekly-report.ts` markdown-таблица секции `По ученикам` заменена на plain text строки с кружками и weekday-метками, а `src/app/attendance/attendance-weekly-report-card.tsx` теперь явно позиционирует отчет как Telegram-friendly plain text вместо markdown-таблицы. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run build` проходит.
- 2026-03-20: формат секции `По ученикам` в `src/lib/server/attendance-weekly-report.ts` обновлен по аналогии со страницей `/attendance`. Markdown-отчет теперь строит таблицу `Ученик | Вторник | Четверг | Пятница | Статус недели` и использует кружки-эмодзи (`⚪`, `🔴`, `🟢`, `🟡`) вместо текстового перечисления состояний. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run build` проходит.
- 2026-03-20: на teacher-only странице `/projects` добавлен weekly Markdown-отчет по статусам проектов. Новый helper `src/lib/server/project-weekly-status-report.ts` строит сводку по текущим project signals, изменениям за неделю и history AI-reports, а `src/app/projects/project-weekly-status-report-card.tsx` показывает готовый текст с копированием для ручной отправки в Telegram; для batch-чтения истории добавлен `listProjectAiReportsByProjectIds` в `src/lib/server/repositories/projects.ts`. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, а `bun run build` проходит.
- 2026-03-20: на teacher-only странице `/attendance` добавлен отдельный Markdown-отчет за выбранную неделю. Новый server-only helper `src/lib/server/attendance-weekly-report.ts` собирает сводку по неделе, списку занятий, пометкам по каждому ученику и зоне внимания, а `src/app/attendance/attendance-weekly-report-card.tsx` показывает готовый текст с кнопкой копирования для ручной отправки в Telegram. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, а `bun run build` проходит.
- 2026-03-19: фоновая синхронизация проектов переведена на GitHub Actions. Добавлен общий helper `src/lib/server/project-sync-batch.ts`, который переиспользуют teacher-only кнопка `Синхронизировать все` и новый защищенный route `src/app/api/github-actions/project-sync/route.ts`. Создан workflow `.github/workflows/project-sync.yml` с `schedule` каждые 4 часа по UTC и `workflow_dispatch`; авторизация route идет через `PROJECT_SYNC_CRON_SECRET`, а endpoint для workflow задается секретом `PROJECT_SYNC_ENDPOINT_URL`. Обновлены `.env.example`, архитектурная документация и memory bank.
- 2026-03-20: автоматический batch `GitHub sync + AI` через GitHub Actions переведен из режима `schedule` в ручной `workflow_dispatch`. Причина: Cloudflare Workers AI на Free-плане исчерпывает дневную квоту `10,000 neurons/day`, поэтому регулярный запуск каждые 4 часа стал неустойчивым. Защищенный route `/api/github-actions/project-sync` и сам workflow сохранены для ручного запуска по требованию; teacher-side кнопка `Синхронизировать все` тоже остается рабочим ручным сценарием. Markdown-документация и memory bank синхронизированы.
- 2026-03-19: устранен сценарий, в котором после teacher-side отвязки ученик все равно повторно привязывал старый GitHub ID из уже открытой OAuth-сессии. В `src/app/login/page.tsx` student bind flow больше не делает автоматический redirect на `/student/link`, если у браузера уже есть сессия; вместо этого показывается подтверждение текущего аккаунта и явное действие `Выйти и сменить аккаунт`. В `src/app/login/login-button.tsx` для student bind flow включен OAuth-параметр `prompt=select_account`, а новый client-компонент `src/app/login/student-bind-session-card.tsx` позволяет безопасно сбросить локальную сессию и повторить вход по той же student-ссылке. Измененные не-Markdown файлы прогнаны через `bunx biome check --write` и повторную проверку Biome; `bun run build` проходит.
- 2026-03-19: teacher-only страница ученика получила отдельную карточку `GitHub link` для исправления ошибочной student bind-привязки. В `src/lib/server/repositories/students.ts` добавлен безопасный сброс `github_user_id/github_username`, в `src/lib/server/telegram-linking.ts` — перевыпуск новой одноразовой GitHub login-ссылки для уже Telegram-подтвержденного ученика, а `src/app/students/actions.ts` и `src/app/students/[studentId]/github-link-card.tsx` дали UI-кнопку `Сбросить и перевыпустить ссылку` с копированием новой ссылки. Архитектурная документация и memory bank синхронизированы. Измененные не-Markdown файлы прогнаны через `bunx biome check --write` и повторную проверку Biome; `bun run build` проходит. Общий `bun run lint` сейчас падает на несвязанном неформатированном `appwrite.config.json` в рабочем дереве.
- 2026-03-19: на `/projects` добавлена teacher-only кнопка `Синхронизировать все`. В `src/app/projects/actions.ts` появился пакетный server action, который выбирает только проекты с `sync_needed`, последовательно выполняет для них `GitHub sync` и автоматический AI-анализ, затем возвращает на список общую success/warning-сводку. `src/app/projects/page.tsx` получил новый CTA в шапке и отдельный banner `sync-all-complete`. Архитектурная документация и memory bank синхронизированы. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, повторную проверку Biome и `bun run build`.
- 2026-03-19: список `/projects` получил live-индикаторы `Repo sync` и `AI status`. В `src/lib/server/repositories/projects.ts` добавлен server-side live-check последнего commit GitHub default branch и сравнение с сохраненным `lastCommitSha`; `src/app/projects/page.tsx` теперь показывает явный статус `нужен sync`, устаревший AI-report и inline-действие `Sync + AI`, которое возвращает feedback прямо на список. Detail page проекта тоже показывает отдельные карточки `Repo sync` и `AI status`. Обновлены `src/lib/types.ts`, `src/lib/server/mappers.ts`, новый helper `src/lib/project-sync.ts`, архитектурная документация и memory bank. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, повторную проверку Biome и `bun run build`.
- 2026-03-19: автозапуск AI-анализа встроен в server actions student/teacher flow. `src/app/my-project/actions.ts` после создания нового student-проекта сразу запускает `runProjectAiAnalysis`, а при временной ошибке сохраняет сам проект и возвращает warning-banner вместо падения сценария. `src/app/projects/actions.ts` теперь после `GitHub sync` автоматически повторяет AI-анализ и показывает success/warning feedback на detail page. Обновлены `src/app/my-project/page.tsx`, `src/app/projects/[projectId]/page.tsx`, архитектурный `docs/README.md` и память проекта. Измененные не-Markdown файлы прогнаны через `bunx biome check --write` и повторную проверку Biome.
- 2026-03-19: парсер `Project Deliverables` в `src/lib/server/project-repository-analysis.ts` расширен до поиска канонических статусов `completed`, `in_progress`, `pending`, `blocked` внутри ячейки, даже если вокруг них есть декоративные префиксы или суффиксы. Теперь teacher-only AI-analysis не должен отбрасывать таблицу deliverables только из-за markdown-оформления статуса. Измененный не-Markdown файл прогнан через `bunx biome check --write` и повторную проверку Biome.
- 2026-03-17: устранено рассинхронизированное состояние project risk flags после AI-анализа. В `src/lib/server/repositories/projects.ts` `normalizeProjectRiskFlags` больше не переносит обратно производные флаги `missing_memory_bank` / `missing_spec` / `missing_plan` / `low_progress` из сохраненного `project_state_json`, если текущие `hasMemoryBank` / `hasSpec` / `hasPlan` и метрики уже обновились. Это снимает кейс, когда teacher-only detail page одновременно показывал `План: есть` в блоке `AI summary`, видимые `Следующие шаги`, но карточка `Риск` продолжала рендерить `нет плана`. Измененный не-Markdown файл прогнан через `bunx biome check --write` и повторную проверку Biome.
- 2026-03-17: стабилизирован teacher-only запуск AI-анализа после multi-project flow. В `src/app/projects/actions.ts` `runProjectAiAnalysisAction` больше не выбрасывает необработанную ошибку в production UI: при сбое пользователь возвращается на detail page с читаемым banner-сообщением, а при успехе получает success banner без generic digest. В `src/app/projects/[projectId]/page.tsx` добавлена обработка `error/success` query state, а в `src/lib/server/repositories/projects.ts` первый AI-анализ нового `draft`-проекта теперь считает `riskFlags` как post-analysis snapshot (`hasAiAnalysisSnapshot: true`), поэтому `missing_plan`/`missing_memory_bank` не маскируются под ложный `healthy`. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`; `bun run build` в этой среде не перепроверялся, потому что локально отсутствуют установленные пакеты `react-markdown`/`remark-gfm`, и это уже зафиксировано как несвязанная проблема окружения.
- 2026-03-17: исправлен Appwrite-фейл `Invalid documentId param` в multi-project flow. В `src/lib/server/repositories/projects.ts` генерация `documentId` для `project_selection_locks` переведена на короткий hash-based идентификатор `psl_<sha256>`, потому что прежний формат `student-project-selection:<studentId>` нарушал ограничения Appwrite по длине и допустимым символам. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` и `bun run build` — все проходят.
- 2026-03-17: дополнительно усилен multi-project student-flow выбора репозитория. В `src/app/my-project/actions.ts` server action больше не выбрасывает необработанные ошибки Appwrite/бизнес-валидации наружу: при конфликте, дубле репозитория, активном текущем проекте или временной блокировке пользователь возвращается на `/my-project` с читаемым текстом ошибки. В `src/app/my-project/page.tsx` добавлены `error/success` banners, чтобы student UI не превращал ожидаемые ошибки выбора проекта в production digest. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run lint` и `bun run build` — все проходят.
- 2026-03-17: устранено server-side падение student-flow `/my-project` при ошибке загрузки GitHub-репозиториев ученика. В `src/app/my-project/page.tsx` загрузка student repositories переведена на безопасный `try/catch`: список проектов из Appwrite продолжает рендериться, ошибка GitHub логируется на сервере, а UI показывает понятное пустое состояние вместо production `500`. Измененный не-Markdown файл прогнан через `bunx biome check --write`, общий `bun run lint` проходит; `bun run build` по-прежнему падает на несвязанной проблеме резолва зависимостей `react-markdown` и `remark-gfm` в `src/components/ui/markdown-content.tsx`.
- 2026-03-16: снят race-condition multi-project сценария. В Appwrite-конфиг и `scripts/provision-appwrite.ts` добавлена коллекция `project_selection_locks`, а `src/lib/server/repositories/projects.ts` теперь берет per-student lock перед созданием нового проекта и переводом проекта в `active`. Одновременно student-проект переведен на стартовый статус `draft`; после teacher-only AI-анализа он автоматически повышается в `active`, если в репозитории подтверждены `hasRepository`, `hasMemoryBank`, `hasSpec` и `hasPlan`. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run lint` и `bun run build` проходят.
- 2026-03-16: подготовлен anti-pause workflow для Appwrite Cloud Free. Добавлен `scripts/appwrite-console-heartbeat.ts` и команда `bun run appwrite:keepalive`, которая открывает текущий Appwrite project Console и пишет локальный heartbeat в `.codex/appwrite-console-heartbeat.json`. Также создан `docs/appwrite-anti-pause.md`, а `docs/README.md` и `memory_bank` синхронизированы с новым operational-паттерном. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, повторный `bun run lint` проходит.
- 2026-03-16: teacher dashboard получил быстрый anti-pause CTA. В `src/lib/server/appwrite.ts` вынесен helper прямого `Appwrite Console` URL, `scripts/appwrite-console-heartbeat.ts` переведен на его повторное использование, а в `src/components/app/teacher-dashboard.tsx` добавлена кнопка `Appwrite Console` в верхнюю action-панель. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: страница `/attendance` переведена на более компактный визуальный язык. В `src/app/attendance/attendance-grid-client.tsx` текстовые кнопки состояний заменены на цветные кружки (`нет данных` — белый, `отсутствовал` — красный, `присутствовал` — зеленый), заголовки дней недели стали кликабельными и массово переключают весь столбец по циклу `unmarked -> absent -> present`, а столбец `Статус недели` теперь тоже рисуется кружком с желтым состоянием при частичном выполнении weekly-нормы. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: на странице `/attendance` убран дублирующий внутренний заголовок `Weekly attendance grid` из карточки-обертки. В `src/app/attendance/page.tsx` оставлен только page-level заголовок `Attendance`, чтобы UI не повторялся по вертикали. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: таблица `/attendance` дополнительно уплотнена и сужена. В `src/app/attendance/attendance-grid-client.tsx` уменьшены ширины weekday-колонок и внутренние отступы ячеек, при этом шрифт заголовков и имен учеников слегка увеличен для лучшей читаемости. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: починена кнопка `Очистить отметки` на `/attendance`. Причина была не в server action, а в том, что `src/app/attendance/attendance-grid-client.tsx` держал `draftStates` только из первого рендера и не пересинхронизировался после `router.refresh()`, поэтому UI оставался на старых локальных значениях даже после очистки базы. Добавлена синхронизация `draftStates` по `rows`, повторные `bunx biome check --write` и `bun run lint` проходят.
- 2026-03-14: в таблице `/attendance` порядок имени ученика в колонке `Ученик` приведен к формату `Фамилия Имя` для一致ия со страницей `/students` и более привычного teacher-view сканирования списка. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: локальная инструкция `AGENTS.md` уточнена по workflow планирования. Обязательное утверждение плана оставлено только для крупных, архитектурных, многосоставных или рискованных задач; для повседневных UI-правок, точечных фиксов и локальных рефакторингов агент теперь должен переходить к выполнению сразу, если запрос пользователя однозначен. Markdown-файл не прогонялся через Biome по локальному правилу проекта.
- 2026-03-14: блок `Массовая Telegram-рассылка` на странице `/students` переработан. В `src/app/students/bulk-notification-card.tsx` список получателей переведен с карточек на компактный data-table только с чекбоксом и ФИО, без `telegram_username` и `chat_id`; scroll area получила общий `app-scrollbar`, а чекбоксы — более аккуратный кастомный стиль. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: в `src/app/students/bulk-notification-card.tsx` уточнена реализация чекбоксов в блоке массовой рассылки. Кастомный стиль через `appearance-none` убран, чтобы не имитировать radio-button и не ломать клик; чекбоксы оставлены квадратными нативными контролами с theme-aware `accent-primary`. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: в `src/app/students/bulk-notification-card.tsx` скорректирован цвет checked-state у чекбоксов массовой рассылки. Вместо системного синего используется явный `accentColor: hsl(var(--primary))`, чтобы control совпадал с палитрой проекта в обеих темах. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: список получателей в `src/app/students/bulk-notification-card.tsx` дополнительно упрощен. Удалены заголовки `Выбор` / `Ученик` и лишний контейнер вокруг scrollable-списка, чтобы блок выглядел компактнее и не дублировал очевидную структуру выбора. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: на странице `/students` упрощен столбец `Посещаемость`. В `src/app/students/page.tsx` удалены текстовая подпись и отдельный `StatusPill`; теперь ячейка показывает только процент посещаемости в одном color-coded badge по `weeklyState`. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: badge посещаемости на странице `/students` слегка укрупнен для лучшей читаемости в таблице. Локально увеличены высота, `padding` и размер текста badge без изменения глобального `Badge` компонента. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: цветовая логика badge посещаемости на странице `/students` уточнена под weekly-норму. В `src/app/students/page.tsx` используется прямой mapping по `attendanceRate`: `0%` — красный (`destructive`), `1-99%` — warning, `100%+` — success. Это соответствует правилу `0 посещений = красный`, `1 посещение = желтый`, `2 и более посещения = зеленый`, потому что норма недели считается от `min(2, lessons.length)`. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: в `src/app/students/page.tsx` усилен визуальный контраст badge для `0%` посещаемости. Красная зона больше не использует мягкий tint и теперь рендерится как явный `destructive` badge с контрастным foreground, чтобы нулевая посещаемость сразу читалась как критический статус. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: в `src/app/students/page.tsx` подача цвета badge посещаемости переведена на явные inline-стили через theme tokens. Это убирает конфликт с `Badge` variant-классами и гарантирует, что `0%` всегда рендерится с красной заливкой, а warning/success зоны — со своей ожидаемой заливкой в обеих темах. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: столбец `Посещаемость` на странице `/students` дополнительно упрощен. В `src/app/students/page.tsx` процентный badge удален полностью; вместо него ячейка теперь показывает только маленький круглый индикатор цвета по weekly-норме (`0%` — красный, `1-99%` — желтый, `100%+` — зеленый) и `title`/screen-reader подпись с точным процентом. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: в `src/app/students/page.tsx` выровнена отрисовка цветных индикаторов посещаемости. Красный статус переведен на тот же явный `hsl(...)`-background паттерн, что и желтый/зеленый, чтобы все три состояния выглядели одинаково по форме и отличались только цветом. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: со страницы `/students` удалена верхняя строка с полями `Поиск по имени, GitHub или Telegram`, `Текущая неделя` и `Teacher view`. В `src/app/students/page.tsx` этот декоративный/нерабочий блок убран, чтобы таблица учеников начиналась сразу с полезного контента. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: после удаления верхней строки на `/students` быстро исправлена регрессия `ReferenceError: Input is not defined` в `src/app/students/page.tsx` — форма `Новый ученик` продолжала использовать `Input`, поэтому импорт был возвращен. Повторные `bunx biome check --write` и `bun run lint` проходят.
- 2026-03-14: из teacher-only карточки `Telegram link` удален блок `Как это работает`, чтобы интерфейс не повторял очевидный для преподавателя flow и оставался компактным. Карточка теперь показывает только статус, invite-ссылку, действия и итог привязки. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: teacher-only `Telegram link` flow на странице ученика упрощен. В `src/app/students/[studentId]/telegram-link-card.tsx` success-модалки убраны, копирование invite-ссылки перенесено в inline-иконку рядом со ссылкой с временной галочкой после клика, а успешные/ошибочные сообщения генерации теперь показываются прямо в карточке без modal interruption. Измененный не-Markdown файл прогнан через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: скорректирован общий UI polish для кнопок и feedback-модалок. В `src/components/ui/button.tsx` тени `default` и `destructive` переведены на симметричный glow без смещения вниз, а `src/components/ui/feedback-modal.tsx` вынесен в `document.body` через portal, получил более плотный overlay с сильным `backdrop-blur`, отдельный full-height слой центрирования по вертикали и горизонтали и полностью непрозрачную карточку, чтобы фон и соседние stacking-context слои больше не просвечивали через модалку. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run lint` проходит.
- 2026-03-14: instructional block на `/my-project` визуально переработан в более продуктовый onboarding-блок. Вместо техничного текстового блока теперь есть hero-like header, три последовательных шага (`AGENTS.md`, `Memory Bank`, запуск prompt) и отдельный CTA с готовым промптом. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run lint` и `bun run build` проходят.
- 2026-03-14: на student-странице `/my-project` prompt подготовки репозитория вынесен из сырого текстового блока в отдельный CTA-блок с кнопкой копирования. Теперь instructional card объясняет шаги, а сам prompt копируется по клику без визуального шума. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run lint` и `bun run build` проходят.
- 2026-03-14: добавлен безопасный teacher-only preview режим для student-flow. Страницы `/student/link?preview=teacher` и `/my-project?preview=teacher` теперь позволяют просматривать student UI на демо-данных из teacher-сессии, не меняя реальную student авторизацию и bind flow. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run lint` и `bun run build` проходят.
- 2026-03-14: student-flow усилен инструкцией по корректной подготовке `AGENTS.md` и `memory_bank`. На `/student/link` и `/my-project` добавлены пояснения, что без правильной структуры Memory Bank AI-анализ проекта и его сигналы будут неточными, а также прямые ссылки на инструкции и готовый prompt для подготовки репозитория. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run lint` и `bun run build` проходят.
- 2026-03-14: модель проектов расширена под несколько проектов на одного ученика. Student page `/my-project` теперь показывает текущий проект и историю завершенных, новый репозиторий разрешается выбирать только после завершения текущего, а teacher-only detail page проекта умеет переводить проект между статусами `active` и `completed`. Сводки по ученикам и teacher dashboard теперь различают активные и завершенные проекты. Измененные не-Markdown файлы прогнаны через `bunx biome check --write`, `bun run lint` и `bun run build` проходят.
- 2026-03-14: обновлена общая button-система в `src/components/ui/button.tsx`. Базовые размеры увеличены, `default/outline/secondary/destructive` стали визуально выразительнее, а кнопки получили единый hover/active motion и тени. Изменение применяется централизованно ко всему UI, использующему общий `Button`. `bun run lint` и `bun run build` проходят.
- 2026-03-14: доработан layout teacher-only project detail page. В карточке `Активность` последний коммит теперь показывается в часах, если с него прошло меньше 24 часов, иначе в днях; правая колонка (`AI summary`, `Следующие шаги`, `История AI-отчетов`) переведена на компактный стек без растягивания по высоте. `bun run lint` и `bun run build` проходят.
- 2026-03-14: брендинг sidebar и favicon унифицирован. Иконка пункта меню `Контроль недели` переосмыслена как отдельный SVG-asset `public/weekly-control-logo.svg`; sidebar header теперь использует этот знак вместо старого `School01Icon`, а `src/app/layout.tsx` явно подключает тот же SVG как favicon/app icon.
- 2026-03-14: production 500 при `POST /projects/[projectId]` на запуске AI-анализа диагностирован как упор в лимит Appwrite для `project_ai_reports.report_payload_json`. Лимит схемы поднят с `12000` до `50000`, `scripts/provision-appwrite.ts` теперь обновляет существующие string-атрибуты по размеру, а `bun run db:provision` успешно применил изменение к текущей базе.
- 2026-03-14: устранен production 500 на `/projects/[projectId]` после AI-анализа. Причина была в `src/components/ui/markdown-content.tsx`: renderer для `code` использовал несуществующую переменную `className` вместо `codeClassName`, поэтому любой markdown с inline code или code block ломал Server Components render. После исправления `bun run lint` и `bun run build` снова проходят.
- 2026-03-14: teacher-only project detail page переведен с `Project brief` на `docs/README.md`, а крупные текстовые блоки (`docs/README.md`, `Product context`, `Active context`, `Progress notes`, `AI summary`, `next steps`) теперь рендерят Markdown через общий UI-компонент. Добавлены зависимости `react-markdown` и `remark-gfm`; `bun run lint` и `bun run build` проходят.
- 2026-03-14: закрыта доработка `PT-07` для teacher-only project detail page. Полные `Project brief`, `Product context`, `Active context` и `Progress notes` теперь сохраняются целиком в сжатом виде внутри `project_ai_reports.report_payload_json -> inputSnapshotJson`; страница `/projects/[projectId]` читает полный snapshot, показывает увеличенные текстовые блоки и поднимает секцию `Прогресс и сигналы` в начало layout. `bun run lint` и `bun run build` проходят.
- 2026-03-14: подтвержден полный production smoke test student-access сценария: `Telegram Start -> GitHub login -> bind -> выбор репозитория на /my-project`. Deliverable `PT-08` можно считать завершенным.
- 2026-03-13: исправлена pre-analysis индикация проектов. До первого AI-анализа teacher-only UI больше не интерпретирует дефолтные `false` в `project_state_json` как реальные `missing_memory_bank` / `missing_spec` / `missing_plan`; вместо этого список проектов, detail page, dashboard и weekly digest показывают статус `данные отсутствуют` до первого repo analysis.
- 2026-03-13: починен student bind flow после Telegram invite. Student GitHub login теперь стартует с landing URL `/login?studentLinkToken=...`, но OAuth callback целенаправленно уходит в `/student/link?token=...`; login page также умеет подхватывать `callbackUrl` от `next-auth`/`middleware` и при уже активной сессии автоматически продолжает bind вместо возврата на экран входа.
- 2026-03-13: AI-анализ проектов переведен с прямого OpenAI Responses API на Cloudflare Worker gateway. Добавлен подпроект `workers/ai-worker` с `GET /health`, `POST /chat`, token-protected доступом и Workers AI `@cf/openai/gpt-oss-120b`; серверный модуль приложения теперь использует `AI_GATEWAY_URL`, `AI_GATEWAY_TOKEN` и `AI_GATEWAY_MODEL`, а документация синхронизирована без изменения `docs/OAuth.md`.
- 2026-03-13: Worker `projects-tracker-ai` задеплоен на `workers.dev`, локальный `.env` привязан к `AI_GATEWAY_URL` и `AI_GATEWAY_TOKEN`, а живой AI-report успешно создан для проекта `-PopFlix88`. Диагностика GitHub API усилена: `403 rate limit exceeded` больше не маскируется под `invalid_github_repo`.
- 2026-03-13: после добавления рабочего `GITHUB_TOKEN` подтвержден authenticated GitHub rate limit `5000`, а teacher-only AI-analysis успешно догнан для `LinguaFlow` и `startlaunch`.
- 2026-03-13: AI-отчеты временно были ужаты под лимит Appwrite `report_payload_json <= 12000`. Позже лимит был увеличен до `50000`, а хранение полного snapshot переведено на сжатый формат.
- 2026-03-13: механизм процента проекта переведен на `## Project Deliverables` в `memory_bank/projectbrief.md`. Старый расчет по `activeContext.md` и `progress.md` удален из server-side analyzer; локальный `projectbrief.md` дополнен взвешенным backlog'ом на `100%` для реалистичной проверки прогресса.
- 2026-03-13: teacher-only страница `/projects` очищена от ручного создания проекта. Источником новых проектов остается student-flow `/my-project`, а detail page `/projects/[projectId]` переделана в обзорный workspace с краткими preview из `memory_bank`, процентом выполнения, текущим контекстом, repo signals и историей AI-отчетов.
- 2026-03-13: на detail page проекта добавлен pending-spinner для server action `Запустить AI-анализ`, а блок `Прогресс и сигналы` получил цветовую индикацию по состоянию прогресса, риска и активности.
- 2026-03-11: зафиксировано завершение ручного production smoke test teacher-only сценариев на Vercel. Подтверждены `/students`, `/attendance`, `/projects`, массовая Telegram-рассылка и teacher weekly digest; следующий шаг перенесен на 2026-03-12 для полного student-access smoke test.
- 2026-03-11: локальный dev server переведен на `127.0.0.1:3300`. Диагностика показала, что в Windows порт `3100` входит в системный excluded TCP range `3068-3167`, поэтому bind на нем запрещен даже вне IPv6. Документация и memory bank синхронизированы.
- 2026-03-13: выполнен минимальный polish страницы входа. На `/login` удалена вторичная ссылка `Архитектура`, а CTA `Войти через GitHub` переведен с самописного `button` на общий `Button` из UI-системы; `Biome` с автоисправлением и повторной проверкой для измененных TSX-файлов проходит успешно.
- 2026-03-12: teacher-only AI-анализ проектов переведен на GitHub `memory_bank` и commit history. Новый server-side parser читает `memory_bank/projectbrief.md`, `productContext.md`, `activeContext.md`, `progress.md` и опциональный `docs/README.md`, считает детерминированный `completion_percent` по уникальным задачам, выставляет rule-based флаги `missing_memory_bank` / `missing_spec` / `missing_plan` / `abandoned`, сохраняет commit metrics в `project_state_json` и `project_ai_reports`, а страница `/projects/[projectId]` показывает repo signals и источники анализа. Обновлены архитектурная документация и memory bank.
- 2026-03-11: ужесточена teacher-auth политика. В production роль `teacher` теперь определяется только по `TEACHER_GITHUB_USER_ID`; `TEACHER_GITHUB_LOGIN` оставлен fallback-механизмом только для non-production сред и локальной разработки. Обновлены `src/lib/server/auth.ts`, архитектурная документация и память проекта.
- 2026-03-11: реализован первый student-access сценарий. Добавлены маршруты `/auth/complete`, `/student/link` и `/my-project`; auth расширен до teacher/student/guest модели, student определяется по `students.github_user_id`, а `/my-project` позволяет выбрать собственный GitHub-репозиторий и создать draft-проект.
- 2026-03-11: Telegram linking flow расширен до GitHub bind flow. После `Start` webhook сохраняет `telegram_chat_id`, генерирует одноразовый `github_link_token`, бот отправляет student login-ссылку, а bind route связывает GitHub-аккаунт с карточкой по `github_user_id`.
- 2026-03-11: Appwrite schema обновлена под student bind flow: в `students` добавлены поля `github_link_token` и `github_link_expires_at`, а также индекс `students_by_github_link_token`; `bun run db:provision` успешно применил изменения.
- 2026-03-11: перепроверена Appwrite-схема для проектов. Подтверждено наличие коллекций `projects` и `project_ai_reports`, всех ожидаемых атрибутов и индексов; `bun run db:provision` проходит идемпотентно без дополнительных изменений в Appwrite. Ограничение `/projects` сведено к пустым данным в production, а не к отсутствующей инфраструктуре.
- 2026-03-11: продолжен read-only smoke test интеграций без браузера. Через Appwrite подтверждены `25` студентов, `3` auto-generated урока на неделю `2026-03-08`, отсутствие attendance-отметок на эту неделю и пустая коллекция `projects` в production. Дополнительно проверены внешние сервисы: `Telegram getMe` для `@dsbdr_bot` успешен, `GitHub API rate_limit` доступен без токена в базовом лимите `60/60`.
- 2026-03-11: устранен шум `LF/CRLF` в рабочем дереве Windows. Для локального репозитория выставлен `git config --local core.autocrlf false`, после чего ложные `modified` по кодовым файлам сняты безопасной пересинхронизацией индекса через `git add`/`git reset` без содержательных изменений. `git status` снова показывает только реальные правки, `bun run lint` остается зеленым.
- 2026-03-11: репозиторий приведен к чистому `biome check` без проверки Markdown-файлов. Выполнено автоисправление `bunx biome check --write` по не-Markdown файлам, после чего успешно проходят `bun run lint` и `bun run build`. В `git status` по многим кодовым файлам остается шум из-за `LF/CRLF` в рабочем дереве Windows, но содержательных diff по коду `git diff` не показывает.
- 2026-03-11: начат production smoke test на Vercel. Автоматически подтверждены успешный `bun run build`, редирект `https://projects-tracker-one.vercel.app/` на `/login`, доступность `https://projects-tracker-one.vercel.app/login` с `200 OK` и защита `POST /api/telegram/webhook` ответом `401` без секрета. Полный teacher-only проход после логина и реальные Telegram-рассылки остались на ручную браузерную проверку.
- 2026-03-10: `attendance` возвращен к batch-режиму: клики по ячейкам снова меняют только client-side draft, кнопка `Сохранить изменения` записывает неделю одним запросом, после чего страница обновляется и weekly status пересчитывается на свежих данных.
- 2026-03-10: production rollout завершен: приложение развернуто на `https://projects-tracker-one.vercel.app`, GitHub OAuth на Vercel стабилизирован через корректный `NEXTAUTH_URL`, Telegram webhook привязан к production route с `TELEGRAM_WEBHOOK_SECRET`, а живая привязка ученика через invite-ссылку подтверждена.
- 2026-03-10: подготовка deployment-контура переведена с Cloudflare на Vercel; временные OpenNext/Wrangler-правки удалены, добавлен `bun run deploy` через `vercel deploy --prod`.
- 2026-03-10: реализован teacher-only Telegram linking flow: Appwrite хранит `telegram_link_token` и `telegram_linked_at`, student detail page умеет выпускать deep-link `t.me/<bot>?start=<token>`, а новый route `/api/telegram/webhook` автоматически сохраняет `telegram_chat_id` после `/start`.
- 2026-03-10: teacher-only таблица `/students` получила отдельную колонку `№`, а `telegram_username` и `telegram_chat_id` вынесены в разные столбцы для более читаемого teacher view.
- 2026-03-10: проведена живая Telegram-проверка на реальных `chat_id` — массовая student-рассылка успешно доставлена двум получателям, а weekly digest успешно отправлен на реальный `TEACHER_TELEGRAM_CHAT_ID`.
- 2026-03-10: teacher dashboard получил teacher-only кнопку ручной отправки weekly digest; добавлены `src/lib/server/teacher-weekly-digest.ts`, server action `src/app/dashboard-actions.ts` и env `TEACHER_TELEGRAM_CHAT_ID`, а итог отправки показывается через `FeedbackModal`.
- 2026-03-10: teacher-only страница `students` получила массовую Telegram-рассылку: добавлен client-side блок выбора учеников, а server action возвращает сводку по `sent`, пропускам без `chat_id`, некорректным ID и ошибкам доставки.
- 2026-03-10: teacher-only уведомления и ошибки переведены с браузерных `alert()` на общий `FeedbackModal`; Telegram-отправка и импорт XLSX теперь показывают стилизованные модальные окна с `success/error` состояниями.
- 2026-03-10: исправлено падение `NotificationCard` после успешной отправки Telegram-сообщения: сброс формы больше не использует `event.currentTarget` внутри асинхронного `startTransition`.
- 2026-03-10: Telegram-поток усилен под реальные `chat_id`: `src/lib/server/telegram.ts` валидирует формат ID и длину сообщения, отключен `parse_mode`, а teacher-only карточка отправки показывает лимит 4096 символов и более точные ошибки.
- 2026-03-10: project-формы и поле `manualOverrideNote` получили явные счетчики символов через `src/components/ui/field-with-counter.tsx`, чтобы лимиты Appwrite были видны до сохранения.
- 2026-03-10: `/attendance` переведен на прямое сохранение по клику: каждая ячейка отправляет одиночный server action, поэтому отметки сразу записываются в Appwrite без общей кнопки сохранения недели.
- 2026-03-10: исправлен порядок расчета `/attendance`: lessons выбранной недели теперь гарантированно создаются/синхронизируются до пересчета student summaries, поэтому `Статус недели` не залипает в `риск` на первом открытии недели.
- 2026-03-10: исправлен расчет weekly status на `/attendance`: summaries учеников теперь считаются по выбранной `weekStart`, поэтому бейдж `Статус недели` совпадает с видимой неделей и отметками в таблице.
- 2026-03-10: `/attendance` получил навигацию по неделям: страница читает `weekStart` из query-параметра, показывает диапазон выбранной недели и позволяет переходить на предыдущую/следующую неделю.
- 2026-03-10: страница `/attendance` упрощена до weekly-таблицы: календарный блок и список нарушителей убраны, а ячейки attendance grid переключают статус одним кликом по циклу `- -> Был -> Не был`.
- 2026-03-10: таблица `/attendance` зафиксирована по трем weekday-колонкам `Вторник/Четверг/Пятница`, чтобы дубли lesson-записей за неделю не раздували число столбцов.
- 2026-03-10: исправлен UTC-сдвиг дат в attendance: `lesson_date` теперь формируется в локальной дате, auto-generated уроки текущей недели синхронизируются с шаблоном `Вт/Чт/Пт`, а dashboard выбирает реально ближайшее занятие.
- 2026-03-10: добавлена server-side нормализация `project_state_json`: ограничены `manualOverrideNote`, `aiSummary` и `nextSteps`, а форма override приведена к тем же лимитам.
- 2026-03-10: устранено предупреждение Biome в `src/components/ui/sidebar.tsx`: состояние sidebar теперь пишется через `Cookie Store API` без прямого `document.cookie`.
- 2026-03-10: project-формы синхронизированы с Appwrite-лимитами, добавлены `maxLength` в UI и server-side нормализация payload через `src/lib/project-limits.ts`.
- 2026-03-10: документация и memory bank синхронизированы с маршрутом `src/middleware.ts`, Telegram-интеграцией и ограничениями полей проектов.
- 2026-03-09: реализована интеграция с Telegram: создан серверный сервис, Server Action и UI-карточка для отправки уведомлений из профиля студента.
- 2026-03-09: реализован импорт студентов из XLSX: добавлен клиентский компонент `ImportStudentsButton` и Server Action для парсинга файлов.
- 2026-03-09: рефакторинг авторизации: `src/proxy.ts` удален, добавлен стандартный `src/middleware.ts`. В `/login` добавлена кнопка `LoginButton` на базе `next-auth/react`.
- 2026-03-09: починен локальный `appwrite_api` MCP server: `.codex/config.toml` переведен с отсутствующего PowerShell launcher на WSL-совместимый `scripts/run-appwrite-mcp.sh`, который читает Appwrite env-переменные из локального `.env`.
- 2026-03-09: в `AGENTS.md` добавлено правило не проверять Markdown-файлы (`*.md`) через Biome.
- 2026-03-09: корень проекта `/mnt/c/Users/Ravva/projects-tracker` добавлен в trusted projects глобального `~/.codex/config.toml`, чтобы Codex начал применять локальный `.codex/config.toml`.
- 2026-03-09: создан базовый `memory_bank`;
- 2026-03-09: добавлен `docs/README.md` как источник архитектурной правды;
- 2026-03-09: завершена синхронизация `docs/PRD.md` с подтвержденными правилами MVP.
- 2026-03-09: инициализирован локальный Git, создана ветка `main`, привязан удаленный `origin`.
- 2026-03-09: добавлен `.gitignore` для исключения секретов и локальных артефактов перед первым коммитом.
- 2026-03-09: архитектурный README перенесен из `local/README.md` в `docs/README.md`, ссылки в документации синхронизированы.
- 2026-03-09: начата реализация UI-фундамента на `Next.js + shadcn`, выбран визуальный вектор `Academic Control Room`.
- 2026-03-09: развернут `Next.js 16` frontend-каркас на `bun`, инициализирован `shadcn` preset `a1F9UU9Q`.
- 2026-03-09: реализован teacher dashboard на маршруте `/`, добавлены app shell, sidebar и базовые metric/data panels.
- 2026-03-09: dev server перенастроен на `localhost:3100`.
- 2026-03-09: реализован teacher-only модуль `students` с маршрутами списка и редактирования.
- 2026-03-09: реализованы первые mock-driven версии `/attendance` и `/projects`.
- 2026-03-09: реализован teacher-only project detail route `/projects/[projectId]`.
- 2026-03-09: добавлен Appwrite-ready data layer, server repositories и `.env.example`.
- 2026-03-09: удален `src/lib/mock-data.ts`, UI переведен на server-side repositories и пустые состояния без mock fallback.
- 2026-03-09: реализованы `next-auth` GitHub OAuth, teacher-only guard и страница `/login`.
- 2026-03-09: добавлены server actions и CRUD-потоки для `students`, `attendance` и `projects`.
- 2026-03-09: создан идемпотентный скрипт `bun run db:provision`, подняты Appwrite-коллекции и индексы.
- 2026-03-09: схема `projects` и `project_ai_reports` переведена на компактные JSON-state поля из-за лимитов Appwrite.
- 2026-03-09: `/login` дополнен явной диагностикой отсутствующих OAuth env-переменных.

## Контроль изменений

- `last_checked_commit`: `d9e0857`
